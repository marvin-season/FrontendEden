{
  "name": "root",
  "private": true,
  "workspaces": [
  ],
  "scripts": {
    "dev": "lerna run --scope @root/js-based --scope @root/shared dev",
    "dev_": "pnpm --filter @root/js-based --filter @root/shared --parallel dev",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
