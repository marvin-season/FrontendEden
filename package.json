{
  "name": "root",
  "private": true,
  "workspaces": [
  ],
  "scripts": {
    "dev": "lerna run --scope @root/js-based --scope @root/shared dev",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
